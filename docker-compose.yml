version: '3.8'

services:
  cap67db:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_PATH=/data/cap67.db
      - RETENTION_DAYS=7
      - STELLAR_NETWORK=pubnet
      - PORT=8080
    volumes:
      - cap67-data:/data
    restart: unless-stopped

volumes:
  cap67-data:
